name = "agent-template"
main = "worker/worker.ts"
compatibility_date = "2024-12-30"
assets = { not_found_handling = "single-page-application" }
preview_urls = true

[observability]
enabled = true

# Optional Worker vars (set in dashboard or wrangler secret):
# CORS_ALLOW_ORIGINS = "https://your-app.example,http://127.0.0.1:5173"  (comma-separated, or * for any)

[[routes]]
pattern = "agent.templates.tldraw.dev"
custom_domain = true

# Set up the durable object used for each tldraw room
[durable_objects]
bindings = [
    { name = "AGENT_DURABLE_OBJECT", class_name = "AgentDurableObject" },
]

# Durable objects require migrations to create/modify/delete them
[[migrations]]
tag = "v1"
new_classes = ["TldrawAiDurableObject"]

[[migrations]]
tag = "v2"
renamed_classes = [
    { from = "TldrawAiDurableObject", to = "AgentDurableObject" },
]
